import { createTheme } from "@mui/material/styles";

export const myTheme = createTheme({
  palette: {
    primary: {
      main: "#6200ea",
    },
    background: {
      default: "#f4f6f8",
    },
  },
});
